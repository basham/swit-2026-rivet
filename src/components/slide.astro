<section>
	<slot />
</section>

<style>
	section {
		align-items: center;
		background-color: var(--rvt-color-theme-background);
		border-block-end: var(--rvt-size-8) solid var(--rvt-color-theme-ui);
		font-size: 4vw;
		justify-content: center;
		padding: 1em;
		height: 100vh;
		width: 100vw;

		> * {
			font-size: inherit;
			line-height: 1.25;
			margin-block-end: 0;
		}

		h1 {
			font-weight: var(--rvt-font-weight-bold);
			font-size: 2em;
		}

		h2 {
			font-weight: var(--rvt-font-weight-bold);
			font-size: 1.5em;
		}
	}
</style>
